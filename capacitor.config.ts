import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'controle-despesas-front-end',
  webDir: 'www'
};

export default config;
